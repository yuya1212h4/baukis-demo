- @title = '職員アカウントの編集'
h1 = @title

div#generic-form
  = form_for [ :admin, @staff_member ] do |f|
    = render 'form', f: f
    .buttons
      = f.submit '更新'
      = link_to 'キャンセル', :admin_staff_members
